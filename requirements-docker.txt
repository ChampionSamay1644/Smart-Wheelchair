# WebSocket server
websockets>=12.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
sounddevice>=0.4.6
noisereduce>=3.0.0

# PyTorch CPU-only (much smaller and faster to install)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchaudio>=2.0.0

# Transformers for Whisper and translation models
transformers>=4.30.0

# Whisper (OpenAI's speech recognition)
openai-whisper>=20230314

# ONNX Runtime for TTS models (CPU only)
onnxruntime>=1.15.0

# Piper TTS
piper-tts>=1.2.0

# Voice biometrics
resemblyzer>=0.1.1

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0

# Utilities
pydub>=0.25.0
